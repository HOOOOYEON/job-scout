---
name: job-scout
description: >
  求职岗位侦察与日报生成。用硬门槛 + 五维评分从招聘信息里筛出真正值得投的岗位，
  并渲染成字段完整、可直接照着投的日报。当用户提到找岗位、求职日报、岗位筛选、
  匹配度打分、公司口碑核实、投递去重、双休/通勤/社保核实、岗位对比或每日求职推送时使用。
  适用于中国大陆校招与社招。
---

# job-scout · 求职岗位侦察

把「今天该投哪几个」这件事，从靠感觉刷招聘 App，变成一套有硬规则、有分数、有证据的流程。

本 Skill 只管**筛选与产出日报**。简历改写、网申填表、投递记录管理不在范围内 —— 那是别的活儿，本 Skill 不越界。

## 硬性边界

- **不猜数字**。薪资、双休、社保、通勤时长都必须有来源。查不到就写「待核实」，不写「大概」「应该」。
- **不把低分岗位凑数**。制定名额是上限不是任务量，不足就如实说不足。
- **不编造口碑**。搜索到的负面评价无法确认归属公司时，必须标「未查到该公司专属评价」，绝不能安到目标公司头上。
- **不替用户投递**。本 Skill 不填表、不点提交、不发邮件。只产出「可以投了」的清单。
- **不做无人值守的主观判断**。定时运行时只做发现、核实、评分、渲染；涉及改简历、改偏好、标记投递的动作一律留到交互式会话。

## 使用前先确认三件事

跑之前必须问清楚，缺一样都会让后面全是无用功：

1. **求职方向**（一到三条线，例如「AIGC 视觉」「新媒体运营」「AI 产品」）
2. **城市与通勤起点**（例如「成都，从天府广场出发，地铁优先」）
3. **硬门槛底线**（哪些条件一票否决 —— 默认是「非双休」和「通勤超时」）

用户没说清就别开始检索。方向越具体，检索命中率越高。

## 主流程

按这个顺序执行，不要跳步：

1. **建立已推已投清单**（去重底座）
   这一步最容易出错，见 `references/dedup.md`。必须多表联查，只查单一来源会漏掉早期直投的岗位。

2. **分线检索候选**
   每条线目标 8–10 个候选，从官方渠道优先（公司官网 / 官方招聘公众号 / 官方 HR 邮箱），聚合平台只能作为发现线索。

3. **查证必填字段**
   按 `references/card-spec.md` 的字段清单逐个查证，一条都不能空着。

4. **先过硬门槛，再算分**
   硬门槛见 `references/hard-gates.md`。被门槛淘汰的岗位**不进入评分环节**，避免「分数很高但根本不能去」的误导。

5. **算综合分并排序**
   评分模型见 `references/scoring.md`。达到及格线的才入围当日清单。

6. **渲染日报**
   结构见 `references/digest.md`。用 `scripts/score.py` 做分数计算与排序，不要手算。

## 三条不能省的自检

交付前逐条过一遍，这三点是最常见的翻车点：

- **去重做全了吗？** 出现在已投记录里的岗位，绝不能出现在「新推荐」位。
- **口碑归属确认了吗？** 同名公司、分公司、关联公司的评价都算「不确定」。
- **每个字段都有来源吗？** 任何一个字段写的是推测，就必须降级为「待核实」并排到末尾。

## 资源索引

- `references/scoring.md` —— 五维评分模型与综合分公式
- `references/hard-gates.md` —— 一票否决条件与边界处理
- `references/dedup.md` —— 多表联查去重，含真实事故案例
- `references/reputation.md` —— 公司口碑检索与归属确认铁律
- `references/card-spec.md` —— 岗位卡必填字段清单
- `references/digest.md` —— 日报渲染结构与排序规则
- `scripts/score.py` —— 综合分计算、硬门槛校验、排序（可直接运行）
- `examples/sample-digest.md` —— 脱敏后的日报示例

## 结果交付

每次交付要讲清楚三件事：

1. **今天推荐了什么**，每条线的头部岗位核心信息（公司 / 岗位 / 薪资 / 通勤 / 双休 / 社保 / 分数）要在正文里直接写出来，不能只丢一个文件。
2. **哪些是确定的，哪些还待核实** —— 把所有写着「待核实」的项单独列一遍。
3. **为什么筛掉了其他岗位** —— 一票否决的原因要说出来，用户才知道规则在起作用。

不要用「已投递」描述还没投的岗位。不要用「应该可以」描述没核实过的条件。
